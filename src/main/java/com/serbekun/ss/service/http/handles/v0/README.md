What do this server layer

Orchestration of the logic of a specific endpoint. They call services, make decisions (what to do, in what order), and return HTTP statuses. They act as a layer between HTTP and business logic.